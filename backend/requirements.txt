# Code View backend — install everything:
#   cd backend && python -m venv .venv && .venv/bin/pip install -r requirements.txt
# Then run: .venv/bin/uvicorn main:app --reload --port 8000
# (Using the venv's Python avoids "ModuleNotFoundError: aiohttp".)

fastapi>=0.104.1
uvicorn[standard]>=0.24.0
websockets>=12.0
python-multipart>=0.0.6

sqlalchemy>=2.0.23
# Required by SQLAlchemy asyncio + conn.run_sync() (e.g. metadata.create_all)
greenlet>=3.0.0
aiosqlite>=0.19.0
alembic>=1.12.1
# asyncpg: only if you switch DATABASE_URL to postgresql+asyncpg://...

# HTTP — required by analysis/scheduler.py (GitHub API, async client)
aiohttp>=3.9.1
httpx>=0.25.2

# Git / graphs / data (live monitoring + future pipeline)
GitPython>=3.1.40
pandas>=2.1.3
numpy>=1.25.2
networkx>=3.2.1

# Config / models
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Parsing (language bindings added when parsers land)
tree-sitter>=0.21.1
